# Only aiogram.fsm (state storage) and its magic_filter dependency are used —
# no Telegram network calls are made; see maxapi/ for the MAX bot client.
aiogram>=3.4,<4.0
aiosqlite>=0.20.0
APScheduler>=3.10,<4.0
certifi>=2024.2.2
httpx>=0.27
openpyxl>=3.1
python-dotenv>=1.0
